version: '3'
services:
  web:
    image: collector
    ports:
      - "9000:9000"
    volumes:
      - ./collector-data/:/srv/collector.torproject.org/collector/data
      - ./collector.properties:/srv/collector.torproject.org/collector/collector.properties
